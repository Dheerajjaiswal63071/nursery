// API Configuration
// Use environment variable if available, otherwise use production URL
const BASE_URL = process.env.REACT_APP_API_URL || "https://nursery-gtlk.onrender.com";

export default BASE_URL;
